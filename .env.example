# Railway Python Build Configuration
# Specifies build environment for optimal compilation

# Use system packages for audio libraries
# This helps avoid compilation issues with librosa dependencies

# Install system dependencies before pip packages
# Railway auto-detects and uses nixpacks

# For better build performance, use prebuilt wheels
PIP_PREFER_BINARY=true
